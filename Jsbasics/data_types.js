string 
number 
boolean 

typeof(null); // is object
typeof(undefined) // is undefined


// a variable doesn't have datatype restrictions 
//it just acts as an container to hold the value


// browsers are intrepreters 
//they use REPL : read , evaluate , print and loop

//number.MAX_SAFE_INTEGER // Gives us the max safe number what we can you in any browsers of the js

//number.MIN_SAFE_INTEGER // Gives us the min safe number what we can you in any browsers of the js
